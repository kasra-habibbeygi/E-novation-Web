import styled from '@emotion/styled';

export const LayoutField = styled.main({
    '& .page_content_field': {
        width: 'calc(100% - 470px)',
        marginLeft: '70px',
        marginTop: '70px',
        padding: '30px'
    }
});
